@model Open.Logic.OrderClasses.OrderLineViewModel
@{
    ViewBag.Title = "DeleteLine";
}

<h2>Delete @Html.DisplayFor(model => model.LineType) @Html.DisplayFor(model => model.UniqueId)</h2>
<h3>Are you sure you want to delete this?</h3>
<div>
    <h4></h4>
    <hr />
    @Html.HiddenFor(model => model.UniqueId)
    @Html.HiddenFor(model => model.OrderId)
    <dl class="dl-horizontal">
        <dt> </dt>
        <dd> </dd>
        <dt> @Html.DisplayNameFor(model => model.UniqueId) </dt>
        <dd> @Html.DisplayFor(model => model.UniqueId) </dd>
        <dt> @Html.DisplayNameFor(model => model.Comment) </dt>
        <dd> @Html.DisplayFor(model => model.Comment) </dd>
    </dl>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "OrderDetails", new {id = Model.OrderId})
        </div>
    }
</div>

